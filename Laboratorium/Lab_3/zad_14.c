#include <stdio.h>

char* JEDNOSCI[] = {"", "jeden", "dwa", "trzy", "cztery", "piec",
                    "szesc", "siedem", "osiem", "dziewiec"};
char* NASCIE[] = {"", "jedynascie", "dwanasie", "trzynasie", "czternasie", "pietnascie", "szesnascie",
                  "siedemnascie", "osiemnascie", "dziewietnasie"};
char* DZIESIATKI[] = {"", "dziesiec", "dwadziescia", "trzydziesci", "czterdziesci", "piecdziesiat",
                      "szescdziesiat", "siedemdziesiat", "osiemdziesiat", "dziewiecdziesiat"};
char* SETKI[] = {"", "sto", "dwiescie", "trzysta", "czterysta", "piecset", "szescset", "siedemset",
                 "osiemset", "dziewiecset"};

int main() {
    printf("Podaj liczbe: ");
    int wartosc;
    scanf(" %d", &wartosc);
    int dziesiatki;;
    int jednosci;
    if (dziesiatki == 1 && jednosci != 0) {
        printf("%s %s", );
    } else {
        printf("%s %s %s", );
    }
    return 0;
}